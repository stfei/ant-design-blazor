# AI 工作入口

本文件适用于整个仓库。开始修改前，先阅读：

1. [项目工作指南](docs/ai-project-guide.md)：项目结构、构建链路、验证命令与通用修改边界。
2. [dev1.6.2 分支说明](docs/ai-branches/dev1.6.2.md)：当前分支相对主分支的功能差异、关键链路和待复核风险。

## 工作规则

- 本仓库的默认主分支是 `master`；需求中提到 `main` 时，除非另有明确引用，按 `master` 理解。
- 先用 `git status --short` 区分用户已有改动，再用 `git diff master...HEAD` 分析当前分支；不要覆盖或顺手整理无关文件。
- 组件功能通常横跨 Razor/C#、TypeScript 互操作、文档 Demo 和测试。修改前先沿真实调用链确认影响面。
- 公共参数或公共 API 发生变化时，同步检查 XML 注释、Demo、文档生成和相应测试。
- 采用与风险相称的最小验证；涉及 TypeScript 互操作时，不能只运行 .NET 测试。
- 回复、任务清单和新增 AI 文档默认使用中文；代码标识符、命令和既有英文术语保持原样。
